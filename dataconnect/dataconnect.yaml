specVersion: "v1beta"
serviceId: "basic-blog"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "basic-blog-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
